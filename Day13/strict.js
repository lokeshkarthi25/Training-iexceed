"use strict" // using this comment can help us  to perform es6 concept in js


 x=100 // before es6 we can define a value without defining the variable type
console.log(x);//so in this case using "use strict" it throughs an error

function add(x){
    return x+x
}
console.log(Boolean(0)); 
console.log(add(2))